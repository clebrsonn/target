spring:
  profiles:
    active: prod
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  data:
    rest:
      basePath: /target

debug: true

logging:
  level:
    web: DEBUG
    org:
      springframework:
        boot:
          autoconfigure: ERROR
